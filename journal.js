// Greet a user by name
function greet(name) {
  return "Hello, " + name + "!";
}

// Check if a number is even
function isEven(n) {
  return n % 2 === 0;
}

// Return the square of a number
function square(n) {
  return n * n;
}